"use client";

import { useCountryStore } from "@/lib/country-store";

/**
 * This component is now just a placeholder or could be removed.
 * Detection logic has been moved to GlobalInitializer.tsx
 */
export function CountrySelector() {
  return null;
}
